/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
        "images.unsplash.com", 
        "nextjs.org", 
        "raw.githubusercontent.com",
        "pbs.twimg.com"
    ],
  },
};

export default nextConfig;
